quite different than non virtual machines can appear anywhere

quantity of resources very between VMs cpu memory storage 

many similarities to physical machines complexity adds opportunity for the attackers

virtualization vulnerbabilities local previldege

the virtual machine is self contained theres no way out or is there?

virtual machine escape break out of the VM and interact with the host OS or hardware

once you escape the VM you have great control

this would be a huge exploit full control of the virutal world

march 2017 Pwn2Own competitioin hacking contest you Pwn it you OWn it along with some cash

javascript engine bug in microsfot edge 

windows 10 kernel bug compromise the guest OS

hardware simulation bug in VM ware escape to the host

the hypervisor manages the relationship betqween physical and virtual resources available RAM space and cpu

these resources can be reused between VMs hypervisor host with 4gb of ram supports three VM with 2gb of ram each RAM is allocated and shared between VMs

data can inadvertently be shared between VMs time to update the memory management feauteres  securityt patches can mitiage the risk

